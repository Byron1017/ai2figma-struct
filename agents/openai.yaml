interface:
  display_name: "AI2Figma Struct"
  short_description: "Editable Figma reconstruction with atomic assets"
  default_prompt: "Use $ai2figma-struct to reconstruct this UI into editable Figma source for development handoff, using atomic assets and pixel-level comparison."

